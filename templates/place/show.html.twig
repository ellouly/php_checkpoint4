{% extends 'base.html.twig' %}

{% block title %}Lieu{% endblock %}

{% block body %}
    <div class="container">
        <h1>{{ place.name }}</h1>
        <table class="table card">
            <tbody>
                <tr>
                    <th>NOM</th>
                    <td>{{ place.name }}</td>
                </tr>
            </tbody>
        </table>
        <div class="button row justify-content-around mt-4">
            <div>
                <a class="btn-show" href="{{ path('place_edit', {'id': place.id}) }}">Modifier</a>
            </div>
            <div>
                {{ include('place/_delete_form.html.twig') }}
            </div>
            <div>
                <a class="btn-create" href="{{ path('place_index') }}">Retour à la liste</a>
            </div>
        </div>
    </div>
{% endblock %}
