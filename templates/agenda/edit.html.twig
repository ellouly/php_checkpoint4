{% extends 'base.html.twig' %}

{% block title %}Modifier date{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier date</h1>

        {{ include('agenda/_form.html.twig', {'button_label': 'Valider'}) }}

        <a class="btn-create" href="{{ path('agenda_index') }}">Retour à la liste</a>

        {{ include('agenda/_delete_form.html.twig') }}
    </div>
{% endblock %}
