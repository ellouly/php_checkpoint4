<form method="post" action="{{ path('agenda_delete', {'id': agenda.id}) }}" onsubmit="return confirm('Etes-vous sûr(e) de vouloir supprimer cette date?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agenda.id) }}">
    <button class="btn-show">Supprimer</button>
</form>
