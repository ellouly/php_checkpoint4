{% extends 'base.html.twig' %}

{% block title %}Modifier catégorie{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier {{ category.type }}</h1>

        {{ include('category/_form.html.twig', {'button_label': 'Valider'}) }}
    </div>
    <div class="button row justify-content-around mt-4">
        <div>
            {{ include('category/_delete_form.html.twig') }}
        </div>
        <div>
            <a class="btn-create" href="{{ path('category_index') }}">Retour à la liste</a>
        </div>
    </div>
{% endblock %}
