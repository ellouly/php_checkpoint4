{% extends 'base.html.twig' %}

{% block title %}Modifier partenaire{% endblock %}

{% block body %}
<div class="container">
    <h1>Modifier {{ partner.name }}</h1>

    {{ include('partner/_form.html.twig', {'button_label': 'Valider'}) }}

    <a class="btn-create"  href="{{ path('partner_index') }}">Retour à la liste</a>

    {{ include('partner/_delete_form.html.twig') }}
</div>
{% endblock %}
