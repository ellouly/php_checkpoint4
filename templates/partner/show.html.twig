{% extends 'base.html.twig' %}

{% block title %}Partenaires{% endblock %}

{% block body %}
<div class="container">
    <h1>{{ partner.name }}</h1>
    <table class="table card">
        <tbody>
            <tr>
                <th>NOM</th>
                <td>{{ partner.name }}</td>
            </tr>
            <tr>
                <th>LIEN INTERNET</th>
                <td>{{ partner.link }}</td>
            </tr>
        </tbody>
    </table>
    <div class="button row justify-content-around mt-4">
        <div>
            <a class="btn-show" href="{{ path('partner_index') }}">Retour à la liste</a>
        </div>
        <div>
            {{ include('partner/_delete_form.html.twig') }}
        </div>
        <div>
            <a class="btn-create" href="{{ path('partner_edit', {'id': partner.id}) }}">Modifier</a>
        </div>
    </div>
</div>
{% endblock %}
