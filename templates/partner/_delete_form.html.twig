<form method="post" action="{{ path('partner_delete', {'id': partner.id}) }}" onsubmit="return confirm('Etes-vous sûr(e) de vouloir supprimer ce partenaire?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ partner.id) }}">
    <button class="btn-show">Supprimer</button>
</form>
