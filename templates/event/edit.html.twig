{% extends 'base.html.twig' %}

{% block title %}Modifier un évènement{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier {{ event.name }}</h1>

        {{ include('event/_form.html.twig', {'button_label': 'Valider'}) }}

        <a class="btn-create" href="{{ path('event_index') }}">Retour à la liste</a>

        {{ include('event/_delete_form.html.twig') }}
    </div>
{% endblock %}
